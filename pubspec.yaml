name: claude_iot
description: "Ce projet vise a develloper une application mobile pour controler les microcontroleurs a locale et a distance avec Bluetooth ou Wi-Fi."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bluetooth_serial: ^0.4.0
  google_fonts: ^6.1.0  # Corrigé: déplacé dans dependencies
  permission_handler: ^11.0.1
  shared_preferences: ^2.2.2
  flutter_bluetooth_serial_ble: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Corrigé: alignement correct

flutter:
  uses-material-design: true